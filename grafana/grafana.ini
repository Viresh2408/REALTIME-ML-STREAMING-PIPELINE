# ============================================================
# grafana.ini — Grafana Configuration
# ============================================================

[live]
allowed_origins = *

[auth.anonymous]
enabled = false

[security]
admin_user = admin
admin_password = ${GRAFANA_ADMIN_PASSWORD}
